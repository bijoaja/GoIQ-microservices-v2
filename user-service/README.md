# User Service

Simple CRUD service for users using Gin + GORM (SQLite).

Run:
  make run

Build:
  make build

Endpoints:
  GET /users
  POST /users
  GET /users/:id
  PUT /users/:id
  DELETE /users/:id
